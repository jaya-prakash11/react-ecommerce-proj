import React from "react";

export default function DesignSystem({ inputName, ...props }) {
  switch (inputName) {
    case "Select":
      break;

    default:
      break;
  }

  return <div></div>;
}
